{
  "scripts": {
    "zip": "git archive -o meta-box-builder.zip --prefix=meta-box-builder/ HEAD",
    "upload": "scp meta-box-builder.zip root@metabox.io:/var/www/metabox.io/downloads",
    "release": "npm run zip && npm run upload"
  }
}
